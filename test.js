// Simulating a simple test
const sum = 1 + 1;
if (sum === 2) {
  console.log("Test Passed!");
  process.exit(0); // Success code
} else {
  console.log("Test Failed!");
  process.exit(1); // Error code
}